name: Keep Repository Active

on:
  schedule:
    - cron: '0 */6 * * *'   # 每6小时自动运行一次
  workflow_dispatch:         # 👈 启用手动触发按钮

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Do nothing, just keep the repo active
        run: echo "🟢 Repository is still alive at $(date)"